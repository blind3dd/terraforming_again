---
# Ansible Galaxy Requirements for Database CI Infrastructure

# Kubernetes collection for k8s module
- name: kubernetes.core
  version: ">=2.4.0"

# AWS collection for AWS modules
- name: amazon.aws
  version: ">=6.0.0"

# Community general collection for additional modules
- name: community.general
  version: ">=7.0.0"

# Community kubernetes collection
- name: community.kubernetes
  version: ">=2.0.0"

# Ansible posix collection for system modules
- name: ansible.posix
  version: ">=1.4.0"

# Community crypto collection for certificate management
- name: community.crypto
  version: ">=2.0.0"

# Community network collection for network modules
- name: community.network
  version: ">=4.0.0"

# Community system collection for system modules
- name: community.system
  version: ">=1.0.0"

# Community docker collection for container management
- name: community.docker
  version: ">=3.0.0"

# Community postgresql collection for database modules
- name: community.postgresql
  version: ">=3.0.0"

# Community mysql collection for database modules
- name: community.mysql
  version: ">=3.0.0"

# Community security collection for security modules
- name: community.security
  version: ">=1.0.0"

# Community monitoring collection for monitoring modules
- name: community.monitoring
  version: ">=1.0.0"

# Community backup collection for backup modules
- name: community.backup
  version: ">=1.0.0"

# Community vault collection for secret management
- name: community.vault
  version: ">=1.0.0"

# Community certificate collection for certificate management
- name: community.certificate
  version: ">=1.0.0"

# Community network collection for network modules
- name: community.network
  version: ">=4.0.0"

# Community system collection for system modules
- name: community.system
  version: ">=1.0.0"

# Community docker collection for container management
- name: community.docker
  version: ">=3.0.0"

# Community postgresql collection for database modules
- name: community.postgresql
  version: ">=3.0.0"

# Community mysql collection for database modules
- name: community.mysql
  version: ">=3.0.0"

# Community security collection for security modules
- name: community.security
  version: ">=1.0.0"

# Community monitoring collection for monitoring modules
- name: community.monitoring
  version: ">=1.0.0"

# Community backup collection for backup modules
- name: community.backup
  version: ">=1.0.0"

# Community vault collection for secret management
- name: community.vault
  version: ">=1.0.0"

# Community certificate collection for certificate management
- name: community.certificate
  version: ">=1.0.0"
